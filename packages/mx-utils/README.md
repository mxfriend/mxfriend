# `@mxfriend/mx-utils`

This package provides a set of utilities helpful during
the development of MXFriend. There's a simple forwarding
utility useful for accessing a mixer over a VPN, and
a scanning utility which can be used to build maps of
exact parameter values for e.g. log-scaled parameters.
